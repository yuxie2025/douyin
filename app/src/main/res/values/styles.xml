<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/QMUI.Compat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#FD702B</item>
        <item name="colorPrimaryDark">#FD702B</item>
        <item name="colorAccent">#F94604</item>
        <!--<item name="android:windowTranslucentNavigation">false</item>//适配华为手机的虚拟按键-->
        <!--<item name="android:textColorPrimary">@color/textColorPrimary</item>-->
        <item name="android:windowBackground">@drawable/screen</item>
    </style>

    <!-- window 透明的-->
    <style name="Theme.AppCompat.Translucent" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
